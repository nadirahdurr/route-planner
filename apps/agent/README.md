# Route Planner Agent Service

FastAPI service that orchestrates the Route Planner MCP engine via a LangGraph ReAct agent. The service will handle terrain bundle ingestion, agent runs, and export orchestration.

## Status

Scaffolding only. Implementation work is tracked in the repository TODOs.
